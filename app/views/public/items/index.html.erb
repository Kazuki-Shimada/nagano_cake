<h1>商品一覧</h1>
<p>ジャンル検索の場合、「商品一覧」の部分は「（ジャンル名）一覧」に変更する</p></br>
<p><例>「ケーキ一覧」</p>
<% @item.each do |item| %>
  <%= link_to public_items_path(item) do %>
    <%= image_tag item.get_image(100, 100) %>
  <% end %>
  <%= item.name %>
  <%= item.price %>
<% end %>